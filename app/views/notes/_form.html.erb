<style>
  .note textarea{
    width:90%;
    height:150px;
  }
</style>

<%= simple_form_for( @note, html: { class: 'note' }) do |f| %>
  <%= f.input :content, as: :text, label: @note.note_type[:label] + ':' %>
  <%= f.submit %>
<% end %>
